# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Shopsi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shopsi
  pod 'Firebase', '~> 5.8'
  pod 'FirebaseRemoteConfig', '~> 3.0'
  target 'ShopsiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShopsiUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
